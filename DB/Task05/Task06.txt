
/*
 * 
 * create Teacher with 
id name salary

create Language with
id name 

Teacher has only Language
Language has many Teacher

- please create classes with relation
 * 
 * 
 * Pls create this tables with relation
----------------------------------------
create Employee with 
id name age

create Phone with
id phoneNumber 

Employee has only Phone
Phone has only Employee
 * */
/*TASK06*/
SELECT * FROM TEACHER;
INSERT INTO TEACHER (TEACHER_NAME,TEACHER_SALARY,LANGUAGE_ID_FK) VALUES ('KHALED0',6000,1);
CREATE TABLE TEACHER (
TEACHER_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
TEACHER_NAME VARCHAR(255) ,
TEACHER_SALARY NUMBER,
LANGUAGE_ID_FK NUMBER,
CONSTRAINT LANGUAGE_ID_FK FOREIGN KEY (LANGUAGE_ID_FK) REFERENCES LANGUAGE (LANGUAGE_ID)
);

DROP TABLE TEACHER ;

SELECT * FROM LANGUAGE;
INSERT INTO LANGUAGE (LANGUAGE_NAME) VALUES ('MATH');
CREATE TABLE LANGUAGE (
LANGUAGE_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
LANGUAGE_NAME VARCHAR(255),

);
